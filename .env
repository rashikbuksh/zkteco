# # Express HTTP port for your server
# PORT=5099

# # Optional: legacy SDK mode (4370) if your device supports it
# DEVICE_IP=192.168.10.2
# DEVICE_PORT=4370
# DEVICE_TIMEOUT=10000


PORT=5099

# Automatically ask device for logs on each getrequest (recommended)
PULL_MODE=1

# Default lookback window if we have no lastStamp for a device (hours)
DEFAULT_LOOKBACK_HOURS=48

# Optional: choose command syntax if your firmware differs
# Supported: DATA_QUERY | GET_ATTLOG | ATTLOG
ICLOCK_COMMAND=DATA_QUERY